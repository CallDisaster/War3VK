# CWorld_RenderScene 未知 Stage 逆向推断（2026-02-14）

## 1. 说明
- 本文用于“未知 Stage 方向”排查，不替代你的实机结论。
- 你给出的实机映射是当前主依据：
  - `S0 天空盒` `S1 地形` `S2 阴影/迷雾/边界` `S6 天气`
  - `S7 选取圈` `S9 地形贴画` `S10 装饰物` `S11 CUnit`
  - `S12 范围染色` `S14 水面` `S18 建筑预览` `S19 路径贴图`
  - `S20 闪电` `S21 漂浮文字`，其余待核验。
- 下面“推断置信度”是基于代码链，不是最终视觉结论。

## 2. 取证函数（IDA）
- `0x6F363020` `RenderWorld_DispatchStage`
- `0x6F76F060` `CWorld_TerrainShadow_Dispatch`
- `0x6F367980`（Stage 15 调用）
- `0x6F368A90` `0x6F369560`（Stage 16 调用链）
- `0x6F76F190` `0x6F7373D0` `0x6F766BC0` `0x6F877740`（Stage 21 相关）

## 3. 未知 Stage 推断表（按你当前未知项）

| Stage | 代码迹象（关键调用） | 推断方向 | 置信度 |
|---|---|---|---|
| S3 | 进入 `TerrainShadow_Dispatch` 子通道（RenderLayer 分支） | 地形阴影/迷雾的子通道之一，偏“地表遮罩层” | 70% |
| S4 | 同属 `TerrainShadow_Dispatch` 相邻子通道 | 与 S3 成对的地表阴影或边界混合 | 70% |
| S5 | `TerrainShadow_Dispatch` 中含 flags 判定后调用 vtbl+36 | 条件型地表 pass（可能与边界或特殊阴影模板相关） | 60% |
| S8 | `TerrainShadow_Dispatch(10)`，走 `RenderListB` 族路径 | 阴影列表 B 的一类附加 pass（可能是特殊覆盖层） | 60% |
| S13 | 该段在反编译链中与世界对象组调用关系紧密（存在版本差异风险） | 可能是“对象组相关的补充 pass / 兼容分支” | 45% |
| S15 | `GetGameUI(...)+588` 虚函数调用 | UI/屏幕空间桥接 pass（非纯世界几何） | 55% |
| S16 | 多个 feature bit 分支，含网格扫描与多入口回调 | 高复杂度复合 pass（可能含路径/提示/覆盖图层） | 65% |
| S17 | `TerrainShadow_Dispatch(11)` | 阴影系统后段子通道（靠近收尾） | 60% |
| S22 | 在 `0x6F363020` 主 switch 未见明确分支 | 保留/未启用/版本差异分支 | 80% |
| S23 | 同上 | 保留/未启用/版本差异分支 | 80% |
| S24 | 同上 | 保留/未启用/版本差异分支 | 80% |

## 4. 与已知映射一致的关键证据
- `S21`：
  - 链路出现 `TextTagFont`（`0x6F766BC0`）和排序绘制流程（`0x6F877740`）。
  - 结论：`S21` 与漂浮文字/文本标签链高度一致。
- `S11/S12`：
  - 在分发器中存在 WorldObjects 组相关调用。
  - 与你“`S11 CUnit`、`S12 范围染色`”的实机观察可并存，说明其内部仍可能复用对象组路径。

## 5. 建议的最小核验顺序
1. 先只开 Stage Debug 单阶段渲染，分别观察 `S3/S4/S5/S8/S16`。  
2. 地图放置“边界+迷雾+路径指示+技能地面贴花”复合场景，优先看 `S16/S19`。  
3. 文本标签密集场景（伤害数字、漂浮字）验证 `S21` 稳定性。  
4. 若 `S13` 仍有分歧，抓一次该阶段内批次 Tag 与 SceneNode 对应日志再定稿。  

