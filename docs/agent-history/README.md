# Agent History Archive

此目录将项目的逐轮交接记录、实验结果和历史计划与根目录 `AGENTS.md` 分离。

- `AGENTS_HISTORY.md`：2026-08-05 迁入的完整旧交接文档，保留原有时间顺序。

日常开发只读根目录 `AGENTS.md`。只有任务依赖某个旧实验的根因、验证数据、回退点或被否决方案时，才按日期/关键词检索此归档，而不是全文加载。

后续新增的长篇变更记录应放在此目录或按主题放入 `docs/research/`、`docs/plan/`；根目录 `AGENTS.md` 只维护稳定概览、当前已知状态、硬约束和入口链接。
