# ✅ 澄清：wire roundtrip 驱动的 `CHECKS=` 是**确定性的** —— 上一轮的并发猜测被证伪 — 2026-09-18

## 1. 我上一轮提出的猜测

round 233 我记录了一个无法解释的差异：同一代码状态下 `CHECKS` 从 1141（round 232）变成 1137（round 233），
并**猜测**可能是场景 E（4 写线程 + 控制线程）的并发调度导致检查数波动，进而提出"检查计数不是稳定量"的担忧。

## 2. 实测：三次同状态运行完全一致

```
run1 exit=0  CHECKS=1137 FAILURES=0   | ROUNDTRIP checks=122 failures=0 PASS
run2 exit=0  CHECKS=1137 FAILURES=0   | ROUNDTRIP checks=122 failures=0 PASS
run3 exit=0  CHECKS=1137 FAILURES=0   | ROUNDTRIP checks=122 failures=0 PASS
```

⇒ **驱动在当前状态下是确定性的。**

## 3. 结论（含对我上一轮说法的更正）

| 我上一轮说的 | 更正为 |
| --- | --- |
| "可能是场景 E 并发导致检查数波动" | **证伪** —— 三次运行完全一致 |
| "检查计数不是稳定量（若写进门禁会不稳定）" | **撤回这一担忧** —— 当前状态下计数稳定 |
| "round 232 的 1141 与 round 233 的 1137 差 4，未能区分" | 现在可确定：**不是运行间噪声**，只能来自**当时那个中间状态与现在确有代码差异** |

**我仍未能定位那 4 个检查的差异具体在哪一处编辑。** 这一点如实保留，不假装已查清。

但可以确定的是：

- **当前状态**（`check_header_block("G", data, expected_version=3)` + 其余三项以注释记录）**可复现且绿色**；
- `CHECKS=1137  FAILURES=0`、`ROUNDTRIP_VERDICT=CERTIFIED_SPEC_SATISFIED`；
- **round 232 的 1141 不可复现**，且它在时间上对应一个**中间编辑状态**（当时我正连续修两个格式串 bug）。

## 4. 方法论：这一轮做对的事

我上一轮**带着一个猜测就报了**（"可能是并发波动"）。这一轮**花一次运行去实测它**，结果把猜测证伪了。

⇒ **对"我猜是这样"的表述，应当先花低成本实验去证伪它，再写进记录。**
这与我此前几次的教训同源：

| 教训 | 场景 |
| --- | --- |
| 比较失败时先怀疑比较本身 | c2 两次 `MATCH: False` 都是我的脚本错 |
| 先落盘再检索 | G 失败行过滤两次没看到，落盘一次就看到 |
| **猜测要先去证伪** | 本轮：并发波动猜测被三次一致运行证伪 |

## 5. 现场

```
本轮未改任何文件（只跑了三次驱动做确定性验证）。
候选 DLL 含：P0 版本门控 / P1 deltaFrames / P1 完成判据按链型 / 写方修正① + 多帧回归锁。
现场站点 = 基线 F275545B…（未部署）；未提交、未部署、未晋升稳定。
```