# 🎯 写方修正② 实机 A/B 验证通过（每项均符合设计预期）— 2026-09-18

## 1. 实验

| | BEFORE | AFTER |
| --- | --- | --- |
| 源码 | 修正① 已合入、修正② 未合入 | **修正①+② 均合入** |
| 候选 DLL | `77CAEED9…` | **`5ADEDD5F…`** |
| 导出 | `cpu-10976-63759690553-1.json`（7,254,952 B） | `cpu-31168-75810535678-1.json`（7,284,895 B） |
| 驱动 | `live_contrast_palette_objects.py` 120 s，`CONTRACT=1`、`INPUTS=0` | 同 |
| 恢复 | `RESTORE OK True` ✅ | `RESTORE OK True` ✅ |

## 2. 结果

```
                    BEFORE      AFTER     设计预期
firstSightInserted     107        64      inserted 应 == emitted
firstSightEmitted       64        64      ——
无链首的对象键          43         0      应为 0（不变量：有条目 ⇒ 链首发过）
假链签名                32         0      应为 0（预算预检提前后不再建"零事件条目"）
解析器链数             107        64      应 == inserted（不再有幽灵链）
stageObservationComplete 32/107    64/64   应显著上升
总 emitted            3691      3648      略降（43 个对象现在完全不可见，不再消耗配额）
terminalEmitted        107        64      应 == inserted
```

「假链签名」定义：`terminal != NoTerminal` **且** `stage == Drawn` **且** `source == NoSource`
—— 即"从未发过链首、只剩终态、被回填成 Drawn"的那种事件（round 254 已确认其成因）。

## 3. 判定：**符合预期**

| 指标 | 结论 |
| --- | --- |
| 无链首的对象键 43 → **0** | ✅ 不变量「表里有条目 ⇒ 链首一定发过」**在真实数据上成立** |
| 假链签名 32 → **0** | ✅ **"用 Drawn 假装被观测过"的链已被消除** |
| 解析器链数 107 → 64 | ✅ 与 `inserted` 一致 ⇒ **幽灵链消失** |
| `stageObservationComplete` 32/107 → **64/64** | ✅ 从 30% 升到 **100%** |
| 总 emitted 3691 → 3648 | ✅ 符合"预算紧张时对象完全不可见"的**已知代价**（且丢失由 `AccountDrop` 显式计数） |

**注意**：`emitted − terminal = 3648 − 64 = 3584 = kNormalBudget`，仍**精确打满** ——
配额仍是约束（这正是 ③ 待做的"配额论证"项）。

## 4. 与修正① 的叠加效果

| 阶段 | 有链首的对象 / 全部对象 | 比例 |
| --- | --- | --- |
| 修正① 之前（更早的实测） | 32 / 107 | 30% |
| **修正① 之后**（round 238） | 64 / 107 | 60% |
| **修正①+② 之后**（本轮） | **64 / 64** | **100%** |

⇒ 修正① 提高链首**覆盖数**（32→64）；修正② 消除**没有链首的幽灵条目**（107→64）⇒
**"解析器看到的每一条链，都真的有链首"**。

## 5. 仍未做/未变

- 43 个对象**完全不可见**：这是修正② 的**已知代价**（不是缺陷），且有显式丢失计数；
- 若要它们重新可见，须**提高预算**（`kNormalBudget`）或降低每对象成本 —— 即 ③ 的"配额论证"项，**须单独论证**；
- `FirstSight` 正名、写方修正③、场景 G 参数化、夹具生产形状：均未动。

## 6. 现场

```
站点     : E:\Work\Warcraft III\d3d9.dll = F275545B…（基线）✅  War3 进程: none
候选 DLL : 36,289,280 B  SHA-256 5ADEDD5FE98228E193B1FB8203BE4F216650BCA3A3508D9C6F9710829A47918D
未提交、未部署、未晋升稳定；候选 ≠ 发布。
```