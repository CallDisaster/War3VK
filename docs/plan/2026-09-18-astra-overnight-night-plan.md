# 今晚无人值守计划 + 进度评估 — 2026-09-18 凌晨（用户离场）

## 1. 进度评估（只量化我能举证的部分）

| 阶段 | 进度 | 依据 | 缺口 |
| --- | --- | --- | --- |
| A 文档更正版 | 100% | 冻结包 7619E195 复核仍不可变；内层四指纹逐项未变 | 无 |
| B 批次1-a 门禁恢复 | 100% | 5 个可失败性探针（1 个哑弹已重做）；场景齐全成硬条件；未放宽判据 | 无 |
| C 批次1-b 链型与 v4 | 列条款 100%；另 1 项新发现语义待裁定 | 见 §1.2 | 双链路由语义（不属原条款，是本次审计新发现） |
| D 批次2 会话协议 | 100% | 单一同步域 / 臂顺序锁 / 头块单次快照 / 屏障用例 | 无 |
| E 受控实机 | 代码半 100%；实机半 0%（仅预备） | 1 处必要修改已做 + 静态锁 + 探针；协议 v4 + 白名单 + 快照装置已实跑验证 | 零实机采集（需授权） |

五阶段等权粗估：(100+100+100+100+50)/5 约 90%。其中 E 的 50% = 代码半 100% / 实机半 0%。

## 1.2 C 的列条款逐条状态

- 共用有界存储与预算、两类链独立不可改类、同一对象可同时持有两条链：完成
- 查找维度 = 对象键 x 链型 x 窗口：完成（含载荷用例 34；但导出层不可见，见待裁定①）
- 两类入口都先 CanEmit 再 Insert：完成（用例 25 / 29）
- 跨帧判序 = 一次明确关联的尝试（attemptSerial 按值携带），不每帧清零：完成（用例 30/31/33 + 生产三点接线锁）
- 终态 ObservationClosed=7（0-6 不动）、Observation 永不 Recovered、Recovered 需真实拒绝事实：完成
- 恒定 v4 写方、v1/v2/v3 仍按各自版本解析、终态表与阶段表按版本校验：完成（两表各有双向行为用例）
- 修我引入的误拒不变量（解析器 710/716 未排除终态）：完成（K1）
- 写方/读方/测试同步 + 分别列项两份测试清单：完成

## 1.3 更远目标：尚未开始，我不编造百分比

本计划产出的是证据层的正确性（前提件）。而「用这条证据链解释 / 修复阴影」这条更远的目标：
实机采集 = 0；实机因果结论 = 0（硬约束禁止在 P0 完成前新增）；阴影是否恢复 = 未恢复且未声称；稳定候选晋升 = 无。

结论：本计划约 90%；更远目标没有可诚实量化的进度。我不能拿"证据层 90%"冒充"阴影问题解决了 90%"。

## 2. 今晚起点快照

门禁：no-work；静态 263/0；meson Ok 85 / Fail 0；宿主机 34/0；wire 原生 ENCODER 73/0 PASS 与 ROUNDTRIP 126/0 PASS；wire 驱动 CHECKS=1160 FAILURES=0；读方两套件 OK。
站点 E:\Work\Warcraft III\d3d9.dll = F275545BAA65A015...5CF07FF3 = 基线，未部署。
包：A 7619E195（冻结）、B 8DDB25E3、C FF68406D、D 436367D6。
审核包：docs/plan/2026-09-18-gpt-review-bundle.md，另有 E:\Work\warvk-gpt-review-20260918.zip。
git：无写操作。

## 3. 今晚无人值守任务（全部不需要用户的决定）

### T1 陈述性断言审计（最耗时，价值最高）

为什么做：本会话已在"注释与代码矛盾"这一类抓到 6 次真缺陷，且每次都是"改行为时补新不删旧"造成的。
范围：我改过的 6 个产品文件 + 2 个读方 + 3 个测试文件里所有注释（不只版本关键词）。
方法：抽取含现在时断言词的注释行（当前/现在/尚未/仍然/恒/必须/已 等），逐条对代码取证（读实现，不靠推断），
      判定"历史叙述（正当）"还是"现状断言（可能已过期）"。
产出：具名缺陷清单（位置 + 原句 + 代码反证 + 判定）；修复（零行为影响，只为误导性断言）；
      每条修复附探针（写得出就写，写不出说明理由）；覆盖界限写入文档。

### T2 写方与读方契约矩阵（逐字段 x 逐版本）

为什么做：本项目缺陷类 #1 是"版本无关用法"（已经 4 次），矩阵能把这类问题结构性地暴露。
范围：paletteObject 头块与记录块的每个槽位与字段（version、watchCount、counters 各键、data[0..8]、words32[*]、
      终态、阶段、链型、窗口段、身份证明种类）x 版本 {1,2,3,4}。
方法：每个格子判定 写方是否产生 / 读方是否接受 / 读方是否拒绝 / 是否有测试覆盖；无覆盖的格子具名列出。
产出：矩阵表 + 未覆盖格子清单 + 对高价值缺口补行为用例（不补字符串锁）。
硬规则：若补用例发现读方与写方真不一致，不自行修，具名记录留待裁定。

### T3 两份测试清单的载荷性核对（若 T1/T2 提前完成）

对两份清单每一项核对是否有可失败探针；没有的具名列出并说明能否补。

## 4. 夜间硬规则（违反即停）

1. 绝不部署 DLL、绝不启动游戏或编辑器、绝不动 E:\Work\War3。
2. 不使用 git 写操作。
3. 不改产品行为，除非有载荷测试配套；只改误导性注释或文档时不改行为。
4. 不得为通过而放宽判据（改期望 / 删断言 / 登记错误期望）。
5. 遇到需要裁定的问题：记录并继续下一项，不自行决定。
6. 每完成一项更新 followup log；门禁若变红必须具名报告。
7. 每次收尾复核站点 DLL 哈希必须仍等于基线。

## 5. 收尾时报告什么

T1/T2/T3 各自：发现数、修复数（是否零行为影响）、覆盖界限、未决项；门禁数字与今晚起点对比；站点哈希；逐条不声称。
