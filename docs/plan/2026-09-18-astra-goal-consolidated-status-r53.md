# WarVK v1.22 计划 · 合并状态（第 53 轮）— 2026-09-18

> Goal `goal-d50cfe33-846f-4837-a630-aad33a0143fd`：phase **active**、revision 1、
> roundsStarted **53 / 400**、activation **armed**。
>
> **本轮不报 blocked**：目标**未整体受阻**（A/B 完成，D 三处缺陷已全部处理）。
> 受阻的是 C 的两项尾项与 D 的 B1 运行期半边，**各自需要一条裁定**。

## 1. 四个阶段的状态

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| **A** 文档更正版 doc-r1 | ✅ 完成 | 内层四指纹逐项未变、外层新 SHA；`ERRATA-doc-r1.md` 撤回 E1/E5/E6/E7/E8、更正 E2/E3/E4/E9/E10；**从未称为代码修复版** |
| **B** 批次 1-a 门禁恢复 | ✅ 完成 | `check_scenario_g` 移出注释、`REQUIRED_SCENARIOS` 硬条件、5 个可失败性探针（P1–P4 + 具名失败打印） |
| **C** 批次 1-b 链型与 v4 | 🟡 **主体完成，两项尾项卡裁定** | 见 §2 |
| **D** 批次 2 会话协议 | 🟡 **三处缺陷处理完毕，B1 运行期半边卡裁定** | 见 §3 |
| **E** 受控实机 | ⬜ 未开始 | 按固定顺序在 D 之后 |

## 2. C 批次：完成项与卡裁定项

**已完成（均有证据）**：两类链独立且不可改类（`FindChain` 链型进匹配谓词，Case 27）；
同一对象同时两条链（Case 27，两终态）；两类入口先 `CanEmit` 再 `Insert`（Case 25/29 + 变异）；
链路归属规则三种形状（Case 27/28）；恒定 v4 写方；v1–v3 按版本解析；终态/阶段表按版本校验；
K1 误拒修复；`ObservationClosed=7`；终态 × 链型矩阵封口（两条规则 + 变异）；
`Observation` 永不 `Recovered`；两类测试清单分列。**K3 机制已就位并有两侧断言 + 变异证明**。

**卡裁定的两项**：

| 项 | 需要什么裁定 | 现状 |
| --- | --- | --- |
| **K3 生产侧接线** | 「**一次尝试**由谁递增」——三候选（`draw` 身份 / 一次 caster 候选集合 / 调用方显式计数器）**均经核对后不可直接用**（序号族是**帧级**；拒绝路径是 manifest 记录、无 caster 索引） | 机制就位、**生产全走未知哨兵** ⇒ 实机判序**仍终身单调**，round 30 的反例**在真实管线上仍成立** |
| **窗口维度** | 是否需要「**保留条目地重新 arm**」的新能力 | 已查明**今天不可观测**（两条 re-arm 路径都清表）⇒ 查找侧**真空满足**、导出侧**已实现并测试**；显式实现已**回退**（无可失败探针的改动不留） |

## 3. D 批次：三处缺陷

| 缺陷 | 处置 | 证据 |
| --- | --- | --- |
| **D1** armed 与计数统一同步域 | ✅ **已修** | 裸 `bool` → `std::atomic<bool>` + release/acquire；4 条静态锁（**因 relaxed 等于没修**）；变异探针具名失败 |
| **D2** 先装钩子再发布 `active` | ✅ **已修** | 钩子块移到所有发布动作之前；新建源码顺序锁（自动入门禁）；变异探针两条断言**同时具名失败** |
| **D3** HeaderJson 同一快照 | ✅ **已满足并加锁** | 单次 `QueryPaletteObjectEvidenceHeader` + 字面版本 4 ⇒ 结构性满足（缺陷原本是 round 10 删掉的**动态版本**）；两个变异探针具名失败 |
| **B1 运行期半边** | 🟡 **卡裁定** | 结构性阻塞查明：**宿主测试目标不链接 sink TU** ⇒ 方向 A（放进 wire 测试，角色混入）**或** B（改 meson，扩大构建面）**是取舍，需裁定** |

## 4. 权威验证（最近一次同轮单跑）

```
STATIC = 261 脚本 / 0 失败        meson = Ok: 85 | Fail: 0        no-work
evidence = 31 passed, 0 failed    wire = CHECKS=1160 FAILURES=0
读方 analysis static = OK         analyze_frame_evidence = OK（不在 261 通配内，手工补跑）
D2 顺序锁 = passed                D3 快照锁 = passed
DLL = 41A70AA50AEABE4245F3D9F640ECC79501D403720AF7F1CE4DACE06A7E9BF951  (36,326,521 B)
站点 = F275545BAA65A0153804AC1CC0B3533B595DA24B5725488B8FFB9DDC5CF07FF3  ← 与基线逐字相同（未部署）
git  = 无写操作
```

**不声称"全门禁通过"**（裁定禁止）：以上是各条**各自**的结果。

## 5. 待裁定清单（按依赖排序）

```
① K3「一次尝试」由谁递增 —— 决定 C 能否闭合、进而决定 D 出包后的 E 是否可用
② B1 运行期半边的落点（方向 A / B）—— 决定 D 能否出包
③ 窗口维度是否需要「保留条目地重新 arm」的新能力
④ 归属规则在其余阶段形状上是否也要显式定义（现只钉了三种形状）
```

## 6. 不声称（全局）

- **不**声称阶段 C 或 D 完成；**不**声称两个 P0 完成 ⇒
  **不新增实机因果结论、不晋升稳定候选**；
- **不**声称任何修复在**实机**上改变了症状（D1/D2 是结构性修复、D3 是已满足 + 加锁；
  **没有**实机观测支持"症状被修好"）；
- **不**把隔离桌面数据当作前台性能；**不**声称阴影已恢复；
- 全局边界：一张表不代表一种事实，一条链结算不代表观察完整，
  观察完整不代表对象已证明，更不代表阴影已恢复。