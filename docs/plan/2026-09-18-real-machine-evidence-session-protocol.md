# ⛔ 本文已作废（SUPERSEDED）— 2026-09-18（round 78）

**不要按本文执行。** 它描述的是一个**已不存在**的部署状态，且它的读取清单**不含**阶段 C 引入的 v4 契约。

| 本文声称 | 当前实际 |
| --- | --- |
| 「现场 DLL（现役）= 36,283,128 B / `519AFA69…`」 | 站点 `d3d9.dll` = **36,288,789 B / `F275545BAA65A015…5CF07FF3`**（= 基线；`519AFA69…` 现在是备份文件 `d3d9.dll.519AFA69_backup_20260918`） |
| 只读三样（子门开关 / emitted / watchCount+dropped） | **不足**：v4 契约还要求核对 `version==4`、`data[4]` 链型、终态 0–7（含 `ObservationClosed=7`）、`windowSegment`、`attemptSerial` 语义 |
| 隐含"已部署、可直接启动" | **站点 = 基线 ⇒ 未部署**；且 AGENTS.md 要求部署/启动须有**用户明确请求** |

**替代文档**：`2026-09-18-stageE-live-capture-protocol-v4.md`。

---
# 实机对象级取证 —— 现场协议（2026-09-18 09:30，DLL 已替换）

## 0. 已完成（主线程）

| 项 | 值 |
| --- | --- |
| 现场 DLL（备份） | `E:\Work\Warcraft III\d3d9.dll.A0A51AF2_backup_20260918-092843` = **36,271,456 B / `A0A51AF2…`** |
| **现场 DLL（现役）** | `E:\Work\Warcraft III\d3d9.dll` = **36,283,128 B / `519AFA69…`**（= 冻结树候选，`ninja -n` no work） |
| 回退脚本 | `E:\Work\Warcraft III\回退到A0A51AF2.cmd`（**专为现场目录写的**；注意旧的 `回退到旧版.cmd` 指向 `E:\Work\War3`，那是副本，别用） |
| 启动器 | `E:\Work\War3-test-build-20260917\启动-取证-含对象级子门.cmd`（设置两个子门环境变量 + 以 `E:\Work\Warcraft III` 为 cwd 启动 YDWE） |

## 1. 你需要做的（两轮，各约 5–8 分钟）

### 第 1 轮：高压中
1. 双击 `E:\Work\War3-test-build-20260917\启动-取证-含对象级子门.cmd`（**必须用这个**，否则对象级子门不生效）；
2. **手动**进单人游戏并载入地图（不要用 `-loadfile`，自动化进不了图）；
3. 打一场**高压**战斗（单位多、技能特效密集），持续 **≥3 分钟**；
4. **在战斗最激烈时按一次 `Ctrl+Shift+C`**（每会话只成功一次）；
5. 等屏幕出现提示后**正常退出游戏**。

### 第 2 轮：压力解除后
6. **重新**双击同一个 cmd（新会话 = 新进程 = 新导出）；
7. 再进**同一张图**，先制造同样强度的战斗，然后**停手让压力回落**（战斗结束/清场后静置 ≥2 分钟）；
8. **在压力已回落时按一次 `Ctrl+Shift+C`**；
9. 正常退出。

> 我需要的差别：**第 1 轮 = 高压中**，**第 2 轮 = 压力解除后**；两轮要同一张图、可比时长。

## 2. 产物在哪（**你不用发给我**）

- 目录：`E:\Work\Warcraft III\WarVK\Log\FrameEvidence\`；
- 文件名：`cpu-<PID>-<nonce>-<generation>.json`（新会话会带**新的 PID**，所以我按时间就能认出新文件）；
- 每个导出还会伴随 `*.json.inputs.bin` / `*.json.inputs.json`（**几十 MB，我不需要**，你不必管）。

### 我会读什么（就三样）
1. `effectiveConfiguration.paletteObjectEvidence` —— **子门是否真的在游戏进程里开启**（09-17 那次失败就是这个为 false）；
2. `paletteObject.counters.emitted` / `terminalEmitted` —— **是否真的产生了对象级记录**；
3. `paletteObject.watchCount` 与 `dropped*` —— 是否有对象在跟踪、是否被丢弃。

判成功标准：**`emitted > 0` 且 `paletteObjectEvidence == true`**（不要求全部计数器非零）。

## 3. 什么时候告诉我

- 两轮都做完后，回我一句「**做完了**」（或告诉我两轮的文件名）即可；
- **若中途游戏崩溃/黑屏/卡住**，立刻告诉我，并**先跑 `回退到A0A51AF2.cmd`**；
- 若你说「做完了」但目录里**没有新文件**，我会判为**导出未触发**（会继续查控制通路），**不会**判成"没有对象"。

## 4. 我不会做的事

- 不在你游戏运行时替换/删除任何文件；不在你游玩期间启动构建或部署；
- 不把这次 CPU 侧导出说成 GPU 提交或画面正确性证据；生产链 `identityNotProven` 是**有意收紧**，不是缺陷。
