# 阶段 C · K3 侦察**合并状态**与决断点（rounds 29-38）— 2026-09-18

> Goal goal-d50cfe33-846f-4837-a630-aad33a0143fd。**无代码改动。**

## 1. 已确定的事实（累计，全部有出处）

| # | 事实 | 出处 |
| --- | --- | --- |
| 1 | 缺陷真实存在：两轮完整尝试后终态是 **Unclosed**（closedRecovered=0） | Case 30 实测 |
| 2 | `Entry::maxStage` **终身单调**，回退 ⇒ `orderViolation` ⇒ 不得判 Recovered | `:763` / `:849-856` |
| 3 | `StageRank` 已修掉**枚举值**问题（FirstSight 枚值 5、语义秩 0） | `:819-847` |
| 4 | 修复**不能**用「每帧清零」（会让同帧内真实回退不再被发现） | 裁定明令 |
| 5 | `PaletteObjectFrames` 的**唯一工厂**在 `war3_palette_object_capture.h:176` | 侦察 |
| 6 | 生产侧构造点**仅 4 处** | 侦察 |
| 7 | 服务路径与拒绝路径的 `recordFrameSerial` **同源**（`renderable.frameSerial`） | `:21516` |
| 8 | 首见路径传的是 `manifestFrame`，是**三者中的异类** | `:23516` |
| 9 | 该序号族是**帧级**（`m_war3ShadowPersistentFrameSerial`）⇒ **不能**作 attemptSerial | `:13711` / `d3d9_device.h:2593` |
| 10 | `replayDrawIndex` 是 **instance** 字段、带 `~0u` 未知哨兵 | `d3d9_war3_scene.h:776` |
| 11 | 服务/首见路径**可**拿到 `replayDrawIndex`（`:21993` / `:23460`）；拒绝路径作用域是 `renderable` | 侦察 |
| 12 | `attemptSerial` **必须带未知哨兵**，`MarkStage` 须把「未知」当作**不重置也不违规** | 本轮首次提出 |

## 2. 唯一剩余未知

**拒绝路径 `renderable` 的类型是否携带 caster 索引（或与 instance 的反向关联）？**

我尝试了三种查法都**没定位到其声明**（该参数在多行签名里）：`Renderable& renderable` 无命中、
`auto& renderable` 无命中、按 `frameSerial` 反查太散。

## 3. ⚠️ 对侦察节奏的自我批评（本轮必须写明）

rounds 29-38 共 **10 轮**，其中：

- **有代码/测试产出的**：只有 round 30（Case 30 诊断用例）；
- **纯文档纪录的**：rounds 29、31-38 共 **9 轮**。

我连续多轮做「读代码 + 写假设」却**没有推进代码**。风险很明确：
**侦察本身变成了回避决断的方式** —— 尤其 round 32/34 我两次因「希望复用现成序号」而绕远，
最终都被证据否掉。

⇒ **下一轮必须是决断轮**：要么查到 `renderable` 的类型（一次定向查证），
要么**直接选候选 C**（调用方显式递增的会话级计数器）。
候选 C 的代价是新增状态，但它的语义**最直接**，且**不受**「哪个现成字段恰好可用」的摆布。

## 4. 下一轮的两种走法（择一，不再继续泛读）

```
走法甲（一次定向查证，限一次工具调用）：
  定位 war3_shadow_renderer_core.cpp:6821 所在函数的签名，看 renderable 的类型；
  若其带 caster 索引 ⇒ 候选 A（改动面：工厂 + 4 调用点）；否则直接转走法乙。

走法乙（不再查，直接实现候选 C）：
  ① PaletteObjectFrames 增 attemptSerial（**带 ~0u 未知哨兵**）；
  ② 工厂增该参数；调用方在一次明确关联的尝试开始时显式给出；
  ③ MarkStage 改为尝试内单调，且「未知」⇒ 不重置、不判违规；
  ④ 两侧断言（两个不同尝试号 ⇒ Recovered；同尝试号内回退 ⇒ 仍违规）+ 反向变异；
  ⑤ 测试侧 MakeFrames 显式接收 attemptSerial（**不得**用帧号冒充）。
```

**我倾向走法乙**：它不依赖任何「恰好可用」的现成字段，语义由裁定直接给出；
即使 `renderable` 后来被证明带索引，改用索引也只是**换一个取值**，不改判序结构。

## 5. 状态（无代码改动）

```
STATIC=259/0 ; meson 85/0 ; no-work ; wire CHECKS=1160 FAILURES=0 ; evidence 30/0
分析套件 105 OK ; DLL 未变（01230C1F…）
站点 : E:\Work\Warcraft III\d3d9.dll = F275545BAA65A015…（基线，未部署）
git  : 无写操作
```

## 6. 不声称

- **不**声称 K3 有任何实现进展（10 轮内只有 Case 30 一个用例）；
- **不**声称候选 A/B/C 中任何一个已被选定（**下一轮决断**）；
- **不**声称 `renderable` 的类型已查明；
- **不**声称窗口维度已进入查找键；**不**声称阶段 C 主体完成；
- 全局边界依旧：一张表不代表一种事实，一条链结算不代表观察完整，
  观察完整不代表对象已证明，更不代表阴影已恢复。