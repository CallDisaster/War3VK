# WarVK 交付包合并索引（A/B/C/D）— 2026-09-18（round 63）

> 四个阶段的包 + 各自的范围声明 + 一致性自检结果 + **同一次实测**的门禁结果。
>
> ⚠️ **关于"验证"的准确说法**：round 61 跑过门禁、round 62 打了包但未重跑；
> **round 63 在打包的同一棵树（DLL 哈希未变）上重跑了全套** ⇒
> 现在"包内哈希"与"门禁通过"**指向同一次实测**，round 62 标注的那个缺口**已闭合**。

## 1. 四个包

```
A  E:\Work\WarVK-delivery-20260918-doc-r1.zip
     32.41 MB   SHA-256 7619E19568F9E0B821BA20164C5C1695CCEFAF6777FDC69E5AC91DEC2D792481
     文档更正版；**裁定要求原包不可变** ⇒ 此后从未改动

B  E:\Work\WarVK-delivery-20260918-stageB-gate.zip
     78,436 B   SHA-256 8DDB25E3642DB7CEC47EFC5A7FCE8B78C75EC75C7E19684318F1E1880997893E
     9 条目；**不含 DLL**（门禁/测试改动，不产出产品二进制）

C  E:\Work\WarVK-delivery-20260918-stageC-chaintype.zip
     8,965,198 B  SHA-256 72813FFB45701CB0FBE2D86C20347626000F6F013F5BB82BB69FBC1B4C041AAB
     20 条目；含 DLL == 树

D  E:\Work\WarVK-delivery-20260918-stageD-session.zip
     8,297,117 B  SHA-256 1C4338B4443AD14317DA31EEB2E9471130421B53AA2433545670D3D90CEF40A0
     15 条目；含 DLL == 树
```

## 2. 同一个二进制（C 与 D 包内）

```
d3d9.dll  B3D08D6D9DE9A044F03192662403BE58B6DBA1D0AA49B55F377D2D5CDB176ED3
          36,333,949 B
```

**范围声明（两包的 MANIFEST 都已写明）**：它是**打包时刻的整树构建**，
同时包含 C（链型 / K3 接线 / 窗口内部守卫）、D（D1/D2/D3/B1）、E（nativeKnown 按值携带）
三个阶段的改动。原因：各阶段在同一构建期内完成，**没有**为出包单独回退其它阶段再重建 ——
那会产出**从未被验证过**的二进制。⇒ **不能**当作"纯某阶段的二进制"。

## 3. 门禁结果（**round 63 单跑，与上述哈希同一棵树**）

```
ninja -C build32 -n              no work to do
AutoTest 静态全量                263 脚本 / 0 失败
meson test                       Ok: 85 / Fail: 0
palette 证据宿主机测试            33 passed, 0 failed
wire 往返（原生跑）               ROUNDTRIP checks=126 failures=0 PASS
wire 往返（驱动 + 场景齐全）       CHECKS=1160 FAILURES=0
读方 analysis static             OK（106 tests）
读方 analyze_frame_evidence      OK（**不在**静态通配内，手工补跑）
```

**这不叫"全门禁通过"**（裁定禁止）：以上是**我跑到的各条各自的结果**。

## 4. 一致性自检（机械核对）

对 B/C/D 三包：逐条读 `HASHES.txt`，**重新计算包内文件**的 SHA-256 并比对。

```
B  7/7   不符 0
C  18/18 不符 0   dll in pkg == 树
D  13/13 不符 0   dll in pkg == 树
```

## 5. 站点核对（硬约束的机械核对）

```
E:\Work\Warcraft III\d3d9.dll = F275545BAA65A0153804AC1CC0B3533B595DA24B5725488B8FFB9DDC5CF07FF3
```
⇒ 与**基线**逐字相同 ⇒ 本计划的所有改动**都只在开发树里**，**未进入玩家站点**。

## 6. E 阶段：**不出包**

E 的**代码半**已落地并验证（原因字段按值携带 + 静态锁 + 变异探针），
但**实机半未做**（需用户明确授权启动，见 AGENTS.md）⇒
出包会把**未完成**阶段伪装成完成（round 50 的结论）⇒ 暂不出包。

## 7. 仍待裁定的两项

1. **窗口维度的导出层可见性**：现在有**记录器内部**的可失败守卫
   （`windowMismatchLookups`，**不上 wire**）；若要让实机排查也能看到，
   需裁定 wire 抬 **v5**（窗口段进条目并导出），或把该计数作为 **v4 counters 新增字段**导出
   （与「一律 v4 + 版本表」冲突）；
2. **E 实机半的启动授权**（AGENTS.md：部署/启动需用户明确请求）。

## 8. 不声称

- **不**声称"全门禁通过"；**不**声称任何修复在**实机**上改变了症状（**零实机观测**）；
- **不**声称包内 DLL 是纯某阶段的二进制；
- **不**声称阴影已恢复；**不**把隔离桌面数据当作前台性能；
- **不**声称两个 P0 完成到可晋升稳定候选（裁定禁止晋升）；
- 全局边界：一张表不代表一种事实，一条链结算不代表观察完整，
  观察完整不代表对象已证明，更不代表阴影已恢复。