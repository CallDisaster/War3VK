# 修订版长期计划（采纳 Astra 审核结论）— 2026-09-18 凌晨

## 0. 我先撤回自己的错误判断（含证据）

Astra 指出「C 已离线完成、D 已完成、只剩六项裁定」的收尾判断不成立。**我独立复核后确认它是对的。**

| # | 我此前的结论 | 实际（我的复核证据） | 性质 |
| --- | --- | --- | --- |
| 1 | 「D2 臂顺序已修」（钩子 < active < Arm 是**应锁住**的顺序） | `war3_frame_evidence.cpp:362 setPreFreezeHook` → `:364 active.store(release)` → `:367 ArmPaletteObjectEvidence` ⇒ **记录器初始化仍在发布之后** | **裁定要求未满足**；我的锁**把问题的一半当成了正确顺序** |
| 2 | 「`align(0,64)` 归一化使零请求守卫不再需要 ⇒ 内存测试只是过时」 | `util_math.h:19 return (what + to - 1) & ~(to - 1);` ⇒ **`align(0,64) = 0`** | **我的第 3 次误诊**（读一眼 `align` 定义即可避免） |
| 3 | 「活源码恰好 3 个工厂调用点」 | 生产实际 **4 个**：`d3d9_device.cpp:22150 / :23528`、`d3d9_war3_shadow.cpp:5241`、`war3_shadow_renderer_core.cpp:6822` | 我的静态锁 **scope 错了**（漏了 `d3d9_war3_shadow.cpp`） |
| 4 | 「E 代码半完成」 | `d3d9_war3_shadow.cpp:5259` 仍用 `draw.paletteDiagnostics.frameTag == 0u` 当 `selectionClearedByNativeOverride` ⇒ **反推未修**；D 点未接 attemptSerial | **高估**：实为"一个采集点的局部改动" |
| 5 | 「ObjectGone 无问题」 | `evidence.h:666 record.stage = PaletteObjectStage::Rejected;` 硬写；`:662 FindOwner` 只处理一条链 | **写方产出自相矛盾内容**（观察链携带 Rejected，而我的读方规则禁止） |
| 6 | — | Astra 实测：`MarkStage` 只更新 `attemptStage`、不更新 `maxStage`，而终态阶段回填用 `maxStage` ⇒ 记过 Drawn 的链回填成 FirstSight | **我尚未独立复核该条**（列入 N2 第一步） |

### 我要改的三处**纪律理解过头**（Astra 指出，我接受）

```
a. 「门禁外 40 个文件从不参与任何门禁」→ 错。它们是「不属于这一个 Python 通配集合」，
   其中 wire 驱动我每轮都用 EXE 跑。正确说法：需逐个登记其执行入口。
b. 「改断言就是放宽判据」→ 过宽。修正确契约时，旧错误的特征化测试**必须有意更新**；
   被禁止的是「为通过而放宽」。我用旧 Case 24 的断言去决定 ObservationClosed 的新语义，
   正是这个错误的实例。
c. 「无法产生行为差异的改动一律不留」→ 会阻止合法的等价重构。正确：
   搬代码要证明**行为相同**；修错误要证明**行为符合新要求**。
```

## 1. Astra 已定下的六项（我采纳，不再等待）

| 事项 | 决定 |
| --- | --- |
| 窗口违规计数是否导出 | **暂不导出、不为它单独升 v5**；窗口查找保护与导出可观测性**分别登记** |
| 双链路由 | **明确关联后的 C1**：逐链准入、损失可见、处理发射导致冻结的重入；**不做**同键无条件广播 |
| E 实机采集 | **本次审核不构成部署或启动授权**；两个 P0 未闭合前不进入实机机制归因 |
| 可复现构建 | **列入独立后续，不阻塞修错**；同时保存源码指纹、构建选项、工具链信息与精确 DLL 哈希 |
| 三个门禁外红 + 内存覆盖 | **不能统一按"过时测试"销账**：内存先建真实方法测试、明确契约并修复；另两条分别核查职责迁移与路径规范化 |
| `firstSightUsed()` | **删动态选版本的用法；保留安全的只读查询函数**；**不再用文本门禁强制该函数存在** |

## 2. 修订后的进度（**下调**）

```
A  文档更正版      完成（冻结包不可变已复核）
B  批次1-a 门禁     完成（探针）
C  批次1-b 链型与v4 **未完成**：ObservationClosed 语义被旧断言绑架、ObjectGone 硬写 Rejected、
                    maxStage 回填缺口、尝试关联未成立（帧号冒充尝试号、D 点漏接、
                    完成标志跨尝试拼接、尝试号未上 wire）
D  批次2 会话协议   **未完成**：active 仍早于记录器初始化；旧会话在途键可进入新会话
E  受控实机         **更弱**：非"代码半完成"，实为**一个采集点的局部改动**；
                    D 点反推（d3d9_war3_shadow.cpp:5259）未修；实机半零进展
```

我此前给的「约 90%」**不成立**，且"六个选项未批准"不是主要距离 ——
主要距离是**若干关键实现仍在用一个看似合理的标签，代替本应明确建立的数据关联**。

## 3. 今晚任务（按 Astra 建议的顺序；至 09:30，项目内部工作按用户授权自行推进）

### N1 · 修会话接纳边界（先做，它是 N2 的前置）

```
目标：初始化（记录器 + 预冻结钩子）**完成后**才发布 active；旧会话在途键不得进入新会话。
                             （依据 war3_frame_evidence.cpp:362/364/367 的实测顺序）
要做：1) 把 ArmPaletteObjectEvidence / 记录器初始化提到 active.store 之前
      2) 接纳时校验键的 sessionGeneration，拒绝旧会话键（并计入具名计数）
      3) 确定性屏障：发布前 / 发布后 / 关闭期间 / 重开期间 四组交错
完成判据：**不是**"armed 是原子的"，而是"这些交错不再产生**无声丢失**或**串会话事件**"
必做探针：撤销修复 ⇒ 新用例必须**具名失败**（并给出实测数字）
```

### N2 · 修链与尝试的完整语义（本轮最大的活）

```
顺序（不可颠倒）：
  a) 复核 MarkStage 与 maxStage 的用途分离（Astra 实测：已知尝试号时 maxStage 不更新 ⇒
     终态阶段回填错误：完整 R/S/E/D 链条却回填 FirstSight）
  b) 修 ObjectGone：不得硬写 stage=Rejected；双链并存时不得只处理 FindOwner 的一条
  c) 修结算规则：**仅有链首后关闭的观察链 → ObservationClosed**（诚实报告缺绘制证据），
     不再因旧 Case 24 的断言把它落进 WindowExpired；**更新新版本测试的正确期望**
     （这是"修契约"，不是"放宽判据"）
  d) 明确"一次入队/绘制处理"的**关联标识**并**按值携带到 D 点**（d3d9_war3_shadow.cpp:5241 是
     第 4 个生产调用点，必须接上）；去重 / 判序 / 完成标志 / 读方使用**同一个关联范围**
  e) 之后才实施双链 **C1**（明确关联后的逐链记录；处理"第一次 Emit 之后第二个 Entry* 可能
     已失效"的重入风险：重新校验窗口状态与条目身份，或采用不会被嵌套结算破坏的发射计划）
完成判据：Astra 的三组实测反例不再复现（终态阶段回填、跨尝试拼接、ObjectGone 伪造阶段）
```

### N3 · 内存契约单独处理 + 可复核交付

```
目标：内存修复**不混入** palette 选择改动。
要做：1) 建**真实方法测试**（抽取 Alloc/AllocLocked/FreeLocked 算法本体）明确契约：零请求应如何、
         被吞的小尾段**是否在释放时归还**（Astra 实测：64MiB chunk 分配 64MiB-64B 后剩余 64B
         被移出自由表，释放只归还请求长度 ⇒ 最终 IsEmpty=false）
      2) 修复或**具名登记为未修**（不得销账）
      3) 下个交付包**必须含实际新版读方、测试与构建配置**（完整树或可精确应用的完整补丁）；
         不得再把"只有局部产品 diff 的文档"称为"无需仓库即可复跑的自包含包"
```

## 4. 今晚**不做**的事（明确边界）

```
· 不做 E 实机采集（Astra：本次审核不构成部署/启动授权；两个 P0 未闭合）
· 不动窗口导出协议（决定：暂不导出、不升 v5）
· 不推进可复现构建改造（列入独立后续，只保存指纹/选项/工具链/精确哈希）
· 不动 palette 准入 / 几何选择 / 渲染优先级
· 不删除 firstSightUsed() 函数本体；但要**移除"强制该函数存在"的文本门禁**
· 不使用 git 写操作；绝不部署 DLL、绝不启动游戏/编辑器
```

## 5. 遗留（Astra 建议下一批之后）

```
· 链实例 / 路径范围区分（诊断 chainId + path scope）：当前同键同链型同窗口内，
  ObjectGone 后重建条目会重置序列 ⇒ 两条链首两条终态无法区分；
  目标是回答"这些记录属于同一条诊断链吗"，**不是**借编号证明 Warcraft 对象身份
  （弱对象身份必须保持弱身份）。
· 尝试号/链实例是否需要上 wire（协议设计问题，按实际需要统一设计，不被"恒定 v4"锁死）。
```
