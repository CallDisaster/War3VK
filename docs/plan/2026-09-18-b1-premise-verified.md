# B1 前提实证：生成器可逐字节复算参考 — 2026-09-18

> 步骤 ③（B1：给生成器加**具名、带版本**的迁移后契约补丁）的**前提验证**。
> 若生成器无法从快照确定性复算出现有 `.inc`，则 B1 不可行。**实测可行。**

## 1. 实验（含备份，可回滚）

```powershell
$inc = src\d3d9\war3\semantic\tests\war3_live_palette_selection_chain_legacy_reference.inc
Copy-Item $inc $env:TEMP\chain_inc_before_b1.inc -Force     # 先备份
py AutoTest\gen_war3_live_palette_selection_legacy_reference.py --m2-2
```

## 2. 结果

```
before SHA : 02FF8AFE5A30E3710D651BC300D1A7BB507B4DF34E6E6E394D6FA3CFD1D6410C
before len : 36025
generator  : generated ... (exit 0)
after  SHA : 02FF8AFE5A30E3710D651BC300D1A7BB507B4DF34E6E6E394D6FA3CFD1D6410C
after  len : 36025
BYTE-IDENTICAL: True
```

### 2.1 意义

| 结论 | 含义 |
| --- | --- |
| 快照 + 生成器 ⇒ **逐字节复现**当前参考 | 参考具备**确定性可重建**与**可审计**属性 |
| 因此"迁移后契约补丁"能被定义为**生成器的一个显式阶段** | 审计者可用同一命令复算，差异来源可枚举 |
| 与"手工改 `.inc`"（原 B2）相比 | B1 **不破坏**该可审计属性 ⇒ B1 确实优于 B2 |

### 2.2 本次实验未改变任何文件

生成器输出与既有文件**逐字节相同**，故工作树内容未发生变化（备份仅用于防覆盖，未触发恢复路径）。
已用 `SHA-256` 双向确认为同一内容。

## 3. 下一步（B1 的剩余实施）

```
3a. 在生成器中新增**具名补丁阶段**，对 --m2-2 抽取出的 chain 本体应用 R1 的规则；
3b. 补丁必须**自带**：名称、依据引用（选择链 :325-332 契约 + 步骤①的行为测试）、版本/日期；
3c. `.inc` 头部 provenance 记录"含哪条迁移后补丁"；
3d. 重新生成 ⇒ 新 SHA；同步**钉死 SHA 的门禁**（5 个静态门禁各自重复该断言）；
3e. 重跑等价门禁与全量门禁（步骤 ④）。
```

> 关键纪律：补丁是**具名且可复算**的转换，不是"放行差异"。逐位等价比较的强度**不变**。

## 4. 进度

```
① 独立测试证明新行为符合预期            ✅
② 生成器快照可得性                      ✅
③ B1 前提（可逐字节复算）               ✅ 本轮
   B1 实施（具名补丁 + SHA 同步）         ⬜
④ 同一检查点重跑全部门禁                 ⬜
```

## 5. 现状与边界

| 项 | 值 |
| --- | --- |
| 静态失败 | 6（等价基线问题未处理） |
| meson 失败 | 2 |
| 门禁期望 / `.inc` | **均未修改** |
| 未提交 / 未部署 | 是 |
| 现场 DLL | `F275545B…`（`519AFA69…` 备份在位）；未改用户视频设置 |
| 目标 | **不可标记完成** |