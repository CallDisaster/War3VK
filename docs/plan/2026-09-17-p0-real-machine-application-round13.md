# P0 对象级证据链 — 实机 pair-0 申请（2026-09-17，round 13）

> 上游 codex 额度已用尽（用户 2026-09-17 指示停止向其汇报）。本申请提交给**用户**裁定；
> codex 最后一轮裁定的顺序要求（并发 → 自动触发与真实载荷 → 去重/序号/版本/Reset → 成本 → 冻结后单独申请）已逐项闭合，见 §3。

## 1. 候选身份（本次构建，未提交、未部署、未晋升稳定）

| 组件 | 字节 | SHA-256 |
| --- | --- | --- |
| `build32/src/d3d9/d3d9.dll` | **36,272,145** | **`EB17FC60F532ACE37F151D69E3A66938D4C52401B8FE8C9A735303891EEB0D40`** |
| 记录器 `war3_palette_object_evidence.h` | 37,796 | （见台账 checkpoint；本树为未跟踪文件，身份按本轮构建计） |
| 转换器 `war3_palette_object_evidence_sink.cpp` | 7,916 | 同上 |
| 导出/头块 `war3_frame_evidence.cpp` | 19,481 | 同上 |
| 解析器 `AutoTest/analyze_palette_object_evidence.py` | 50,222 | 同上 |
| 往返编排 `AutoTest/test_palette_object_wire_roundtrip.py` | 70,845 | 同上 |

**注意（身份纪律）**：本树 PE 时间戳使每次重链接后 SHA 变化（同源码不同 SHA，字节数稳定）；
因此实机前必须**冻结**（不再构建）并以本节身份为准。测试现场当前仍是**更早**的冻结测试版
`D1C3EBBB…`（36,253,553 B）——若批准 pair-0，需要一次**显式的部署授权**（现场替换 + 时间戳备份 + 回退脚本已在位）。

## 2. 与上一实机候选的差异

上一实机组合候选为 `8CECC495…`（35,984,240 B）。本候选在其之上新增（全部为**诊断路径**或默认关闭项，另有生命周期加固）：
- 对象级证据链：有界记录器（1024 槽 + 16384 位布隆前置否定 + 强混淆哈希）、三点采集（R/S/D）、唯一转换点、导出头块（**version 2**）；
- 并发所有者协议（记录器状态锁、递归锁防重入死锁）、预冻结钩子（自动冻结前结算终态）、D 点独立小型按值诊断载荷；
- 去重载荷口径（`droppedPayloadConflict`）、解析器恒等式/认证谓词/序号严格性/版本化/窗口分段；
- 成本测量与由其驱动的两处退化修复（哈希聚集、满表负查找）。

## 3. 前置闭合（上游裁定的顺序，逐项附证据）

| # | 要求 | 状态与证据 |
| --- | --- | --- |
| 1 | 并发所有者与关闭协议 | **闭合**：`StateLock` 串行化全部状态访问；关闭协议＝持锁停止接纳→持锁结算→再冻结环；场景 E（4 线程 1600 次并发 + 400 次与冻结竞争，恒等式全成立、连跑 7 次全绿）；变异 M39/M40 被杀 |
| 2 | 自动触发路径与真实载荷交接 | **闭合**：`PreFreezeHook` 覆盖 post-window 自动冻结（场景 F，导出 `reason==2` 且含终态）；D 点改用独立小型按值诊断载荷（不再依赖 `InputsEnabled()`），门禁 §11o 钉死 |
| 3 | 去重/序号/版本/Reset 反例 | **闭合**：载荷冲突计数 `droppedPayloadConflict`（M42 被杀）；序号严格 1,2,3…（相等/跳号/缺头都拒绝）；显式版本化（版本 1 旧合同逐字段不变、未知版本拒绝）；窗口分段 `data[3]`（Reset=1、换图递增）与身份证明种类 `bits[15]`，反例修前/修后都实测 |
| 4 | 成本测量 | **闭合**：容量/最坏探测/六个触发点/零堆分配/关闭开销/开关开销，见 `docs/plan/2026-09-17-p0-object-evidence-cost-measurement.md` |
| 5 | 冻结后单独申请 pair-0 | **本文件** |

## 4. 建议的实机方案（pair-0）

- 现场：`E:\Work\War3`（**玩家目录 `E:\Work\Warcraft III` 绝不触碰**）；客户端区 1902×963；
- 一个 **contract ON / OFF 配对**（子门 `DXVK_WAR3_FRAME_EVIDENCE_PALETTE_OBJECT=1` 仅在取证轮开启），**不重跑**、不挑结果；
- 目标是取回**生产采集点**的真实记录（当前所有生产链在版本 2 下都会具名 `identityNotProven` —— 这是**有意的**：三个采集点没有实例生命周期证明）；
- 观测项：`droppedPerFrame`/`droppedPerSession`/`droppedTableFull`/`droppedPayloadConflict`/`ringEvictedAfterRecord`、链闭合情况、窗口分段是否正确；
- 产物：`WarVK\Log\FrameEvidence\`（含 CPU/inputs JSON 与 manifest），由 `analyze_palette_object_evidence.py` 与通用读方离线复核。

## 5. 明确不承诺的事（不得被读成已解决）

- 本候选**没有任何实机运行**；旧哈希（`8CECC495…`）的实机结论**不继承**；
- 生产路径**不会**认证同对象恢复（无实例生命周期证明）；"同对象已恢复"必须由 identityProven 为真才能说；
- Served/Enqueued/Drawn 分别只是「CPU caster 候选入队」「绘制命令已记录」，**不是** GPU 提交完成、不是像素证明；
- 槽位所有权证明（路由 A）未获批准；构建线程/写者关系只有源码级与静态判定，无运行时见证；
- 未提交、未部署、未晋升稳定版。
