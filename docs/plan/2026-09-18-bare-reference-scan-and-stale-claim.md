# 裸写引用扫描 + 一处**陈旧现状断言**（`chainType` 已参与查找）— 2026-09-18（round 83）

> 起因：round 82 的方法**只覆盖反引号引用**，而 round 81 那个真缺陷（`见 CloseWindow 的版本 3 分支`）
> 是**裸写**形式 ⇒ 本轮专门扫裸写引用，并**逐条看限定语**（问题藏在限定语里）。

## 1. 方法

```
在我改过的 6 个文件（产品 4 + 读方 2）的**注释行**里抽取裸写引用：
    /(见|参见|see)\s+([A-Za-z_][A-Za-z0-9_:]{3,})/g
再逐条验证：①被引对象是否存在 ②**限定语**（引用之后那半句）是否仍成立
```

## 2. 结果 A：14 处裸写引用**全部成立** ✅

| 位置 | 引用 | 验证 |
| --- | --- | --- |
| `evidence.h:11` | `RecorderConfiguration::paletteObject` | 存在（src .h=5 / AutoTest=112） ✅ |
| `:79` | `StageRank()` | 存在（4 / 2） ✅ |
| `:137` | `Entry::chainType` 说明 | 存在 ✅ |
| `:361` | **`Case 8 的 4 处失败`** | `RunCase("8 duplicate reject in one frame", …)` 在测试 `:3675` ✅ |
| `:848` | `CloseWindow` | round 81 已修（原写"版本 3 分支"，不存在） ✅ |
| `:1069` | `StateLock 说明` | 存在（26 / 21） ✅ |
| `:1075` | `NoteRingEviction` | 存在（2 / 8） ✅ |
| `:1077` | `Find 说明` | 存在 ✅ |
| `capture.h:185` | **`Case 30 的哨兵契约锁`** | `RunCase("30 diagnose second-attempt ordering (K3)", …)` 在测试 `:3708` ✅ |
| `frame_evidence.cpp:71` | `analyze_palette_object_evidence.py` | 文件存在 ✅ |
| `frame_evidence.cpp:82` | **`docs/plan/2026-09-17-p0-…md §4 D2`** | 文件存在 ✅、含 `§4` ✅、`D2` 标题在 `:152` ✅ |
| 读方 `:444` | `LEGACY_STAGES 上方注释` | 存在 ✅ |
| 读方 `:451` | `reserved_data_indices` | 存在（AutoTest=14） ✅ |
| 读方 `:615` | `see exactly` | **不是引用**（自然语言）— |

**最高风险的三类都专门查了**：①文档文件 + **章节号**（`:82`）②**测试用例编号**（`Case 8`、`Case 30`）
③**同一文件的另一处注释**（`:137/:1069/:1077`）。全部成立。

## 3. ★ 结果 B：发现并修一处**陈旧的现状断言**（同类第 6 次）

```cpp
// evidence.h:364（修前）
// 不能先用"拒绝/放弃"过渡。当前 `chainType` 只是**待用基础设施**，尚未参与查找。
```

**取证（先证据后判断）**：

```
participates: :972  FindChain 谓词  if (SameKey(e.key, key) && e.chainType == type)
              :353/:451/:994/:996  FindChain 调用点（两类入口 + FindOwner）
              :307  CloseWindow 按 chainType 分档（ObservationClosed）
covered by:   宿主机用例 27（两链独立）/ 28（单链形状）/ 35（双链路由）
```

⇒ "**尚未参与查找**" **已不成立** ⇒ 已改为标注**已落地**（并保留"曾被改写前长期写着已过期内容"的事实）：

```cpp
// **已落地（2026-09-18 阶段 C）**：`chainType` 现已**参与查找与结算** —— …
// 上句的"必须同时落地"因此**已满足**；本条注释在被改写前曾长期写着"尚未参与查找"（已过期）。
```

⇒ 这一处的危害比前几处更大：它是一句**明确的现状断言**（"尚未参与查找"），
  会让读者以为两链机制**还没接上**，从而去重复实现或误判 Case 27/28/35 的覆盖意义。

## 4. 顺带判断：被引用的 `2026-09-17` 文档**不需要处置** ✅

`frame_evidence.cpp:82` 引它为**依据**；而它内部 `:239` 仍写「D1/D2 待裁定」—— 看似过期。
但它开头**自我限定**：

```
（2026-09-17）本文是**方案与论证**，不是状态本身；实现与门禁见文末。
```

⇒ 一份**声明自己不是状态**的方案文档，写它写作时的"待裁定"是**正当**的 ✅。
⇒ **我因此没有给它加横幅**：那会是**范围外的改动**，且违反"取最小侵入"的自律。

## 5. 覆盖界限（诚实标注）

- 只扫我改过的 **6 个文件**的注释；`src/` 与 `AutoTest/` 其余文件**未扫**；
- 只匹配 `见/参见/see` 三种引导词 + 其后一个标识符；**其它引用形态**
  （如"（上文）"、"同 §x"、"前文"）**未覆盖**；
- 我只验证了"被引对象存在"与"**我能读出来的**限定语是否成立"；
  **限定语语义**的判断依赖人工阅读，可能仍有我看错的情况；
- 我上轮的文档扫描只覆盖 `2026-09-18-*.md`；本轮虽触及一份 `2026-09-17` 文档，
  但**更早日期的文档整体仍未扫** ⚠️；
- "结果 A 全部成立"**不**等于引用都恰当（可能引了一份与论点无关的文档）。

## 6. 状态（本轮只改 1 处注释，零行为影响）

```
palette 宿主机测试   34 passed, 0 failed
wire 驱动            CHECKS=1160 FAILURES=0
站点 = F275545BAA65A015…（基线，未部署）   git = 无写操作
```

## 7. 不声称

- **不**声称全树没有悬空/陈旧引用（见 §5 的边界）；
- **不**声称结果 A 的 14 处引用都**恰当**（只验证了对象存在与限定语成立）；
- **不**声称本轮修的注释曾导致**任何**错误行为（**零行为影响**；危害是误导读者/改者）；
- **不**声称更早日期文档已扫（**未扫**）；
- 全局边界：一张表不代表一种事实，一条链结算不代表观察完整，
  观察完整不代表对象已证明，更不代表阴影已恢复。