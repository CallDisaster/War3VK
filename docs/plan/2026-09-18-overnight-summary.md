> ⚠️ **历史文档 —— 已过期（2026-09-18 round 79 标注）**
>
> 本文描述的是**当时**的现场与计划。**当前权威事实**见 `2026-09-18-objective-evidence-ledger.md`：
>
> ```
> 站点 E:\Work\Warcraft III\d3d9.dll = 36,288,789 B / F275545BAA65A015…5CF07FF3 = 基线 ⇒ **未部署**
> 写方 = **恒定 paletteObject version=4**（已删除 firstSightUsed() 动态选版本）
> 实机采集 = **未获授权、零采集**（协议见 2026-09-18-stageE-live-capture-protocol-v4.md）
> 门禁 = 静态 263/0（**不含**门禁外 40 个文件里的 3 个既有红）
> 五项裁定待决（窗口维度导出层 / 双链路由 / 实机授权 / 构建可复现 / 门禁外三红与覆盖缺口）
> ```
>
> **若本文与上述冲突，以上述为准。** 尤其是：本文若说"现场 DLL 已部署/勿动"，
> 那是**当时**的状态；**今天站点是基线、未部署**。
>
> ---
>
# 2026-09-18 夜间执行总结（DeepSeek 主线程）

> 执行窗口：2026-09-18 01:18 → 07:30（计划见 `docs/plan/2026-09-18-overnight-execution-plan.md`，交接见 `docs/plan/2026-09-18-deepseek-overnight-handoff.md`）。
> 本文件由主线程撰写；**所有验收数字均由主线程在本机实跑取得**，子代理证据已逐条标注。
> 纪律：未 git 写、未部署、**现场 DLL 全程未替换**、未称稳定版、无编造结论。

## 0. 一句话结论

夜间把 **③ M2-3、④ M2-4、M2-5 前置、M2-5 余项、A9/A10、⑤ Registry domain 隔离第一块** 全部做完并由主线程独立验收；**① T1 实机对象级取证彻底卡住**（三种启动形式都进不了图 + 沙箱无游戏），② T8 随之阻塞；⑤ 第二块（域计数对外出口）**也已完成并验收**。

## 1. 最终树状态（主线程实跑）

| 项 | 值 |
| --- | --- |
| `ninja -C build32 -n` | no work |
| 预算门禁 | EXIT 0；冻结符号 **157**；行首站点 迁移前 165 → 上限 112 → 当前 **112**；已迁出 **46**；device.cpp 行首站点 **567** |
| 等价门禁 | **七条全 EXIT 0**（M1 / M2 / M2-3 / M2-5 / M2-4 / M2-5B / A9）+ 新增域门禁 EXIT 0 |
| meson | **Ok: 84  Fail: 0**（83 → 84） |
| 全量静态 | **256/256**（249 → 256） |
| 生命周期 | **187/187** |
| 往返 | **主线程亲跑：CHECKS=1107 FAILURES=0 / CERTIFIED_SPEC_SATISFIED（A–F）** |
| **记录器 23/23** | **主线程亲跑**：`war3_palette_object_evidence_test.exe` ⇒ SUMMARY: 23 passed, 0 failed |
| **成本 PASS(38/38)** | **主线程亲跑**：`war3_palette_object_evidence_cost_test.exe` ⇒ COST_VERDICT=PASS checks=38 failures=0 |
| 帧证据运行时 | **主线程亲跑**：`runtime checks=723 accepted=10000 explicitLosses=0 PASS` |
| 解析器 94/94 · 根读方 55/55【⚠️ 夜间加注「无原始输出出处」**已在 §5.7.2 撤销**：一手复跑 55/55 OK，可引用】 | 子代理证据起步，次班已一手复跑确认（§5.7.2） |
| 仓库 DLL | **36,283,128 B / `519AFA6904F47E9C2E168FF0E5E0A17A470B27F9F9E714783D99C0333E397306`** |
| **现场 DLL** | **`A0A51AF2…`（未替换，全程保持）** |

DLL 身份沿革（每步均为该步最终树的最后一次真实构建）：`0090B354…`（M2-3）→ `34FB05C5…`（M2-5 前置）→ `BDB47E4E…`（M2-4）→ `7DA555A7…`（M2-5 余项）→ `824AA63B…`（A9/A10）→ `6D17B038…`（Registry domain 隔离）→ `295C7FD2…`（域拒绝计数对外出口）→ `519AFA69…`（出口块三条变异补强后重链）。

## 2. 逐任务结论

### ⑤ 域拒绝计数对外出口（第二块）—— **有证据（已完成并验收），但未完成**
6 个域拒绝计数接到既有出口链（bridge → perf monitor → 两个 JSON 写手 + series 6 列）；静态出口门禁 14 tests + 定向测试 5 tests；2 条变异真跑。**注**：出口走 `war3_perf_report.html` 内嵌 JSON，不是 `runtime_status.json`（后者实测 0 命中本族）。**未做到**：出口代码未被可执行级测试覆盖、「实机 JSON 里确有这些键」未验证；文档 `2026-09-18-registry-domain-count-export-record.md`。

### ③ M2-3 motion 三函数迁出 —— **有证据（已完成并验收）**
FROZEN_M2_3 先补（命名族漏检）；legacy 参考 + 宿主差分 + 2 条变异真跑 + 全门禁复跑；文档 `2026-09-18-m2-3-migration-equivalence-record.md`。

### ④ M2-5 前置（taxonomy 发射块抽纯函数）—— **有证据（已完成并验收）**
`device.cpp :21202-21580`（379 行）迁出到新模块 `war3_palette_taxonomy_emission.{h,cpp}`；34 字段逐点等价；差分 30,443,383 条断言 0 DIFF；新增 `FROZEN_M2_5`；文档 `2026-09-18-m2-5-taxonomy-extraction-record.md`。

### ④ M2-4（A1–A8 共 8 符号 / 9 定义）—— **有证据（已完成并验收）**
新增 `FROZEN_M2_4`（修补判定 3 盲区）；legacy `.inc` 9,397 B；差分 7,477,257 条断言；A9/A10 死代码**未夹带**；文档 `2026-09-18-m2-4-migration-equivalence-record.md`。

### ④ M2-5 余项（B4 迁出 + B 表其余逐条裁定）—— **有证据（已完成并验收）**
B4 迁出到 `war3_palette_submitted_aggregation.{h,cpp}`（差分 1,347,808 条）；**B1/B2/B5/B11 判为不可做并给出源码锚理由，未硬做**；B6–B10 为非独立项；文档 `2026-09-18-m2-5-remainder-record.md`。

### ④ A9/A10 死代码裁定落地 —— **有证据（已完成并验收）**
A10 删除（−369 B，捕手 = M2-4 门禁文本缺席断言）；A9 迁出（−546 B，legacy `.inc` 3,026 B，差分 947,458 条，新增 `FROZEN_A9`）；文档 `2026-09-18-a9-a10-dead-code-ruling-record.md`。

### ⑤ Registry domain 隔离（第一块）—— **有证据（已完成并验收），但未完成**
显式 `ShadowGeometryDomain`（取值沿用 0x53310001 / 0x53314301，逐位不变）+ **8 处 fail-closed owner-check** + 3 入口 8 调用点携带 domain；宿主域测试 **46/46**（41 → 46）；变异证明「两 domain 同值」是**编译错误**；文档 `2026-09-18-registry-domain-isolation-record.md`。
**D 项**：D2/D8 闭合；D6 部分；**D1/D3/D4/D5/D7 未动**。**F 项**：F1-1/2/3 达成、F2-4 部分、F2-6 达成、F4-10 达成；**F3-7/8/9 全部未执行**（本树无 pre-U5 基线、未启动游戏）。

### ① T1 实机对象级取证 ×2 —— **未覆盖（卡住，非未尝试）**
三次启动尝试全部失败：`YDWE.exe -war3 -loadfile`（开成世界编辑器）、`YDWEConfig -launchwar3 -loadfile <绝对>`（停主菜单）、**`war3.exe -window -loadfile <相对>`（AutoTest 源码的权威形式，标题 200 秒恒为 `Warcraft III`）**。
根因判定：**未进图 ⇒ `InitializeRuntimeCore()` 不跑 ⇒ 控制面管道不存在**（与 09-17 的直接观察一致）。
沙箱路线亦不可用：`preflight_instance_pool` 实测 **`ok=false`**，因为 `E:\Work\War3_AutoTestSandbox` **没有游戏安装**。
**结论**：T1 在本机当前配置下**无法自动采集**；需 (A) 人工进图按一次 `Ctrl+Shift+C`（高压中 + 压力解除后各一次；09-17 20:12 的人工导出已证明该路线可用），或 (B) 授权把完整游戏供进沙箱后走全自动 `launch_war3_instance`。
**判定文档 `2026-09-18-p0-real-machine-execution-record.md` 未创建**——没有任何新证据可判，不写空文档、不编造结论。

### ② T8 双图性能基线 —— **未覆盖（已做有界重试，有原始输出）**
实测 `py AutoTest/dual_perf_baseline.py`（**EXIT=0**）：HIGH/LOW 两段均 `ok=False stage=launch`，所有指标为 **0**（脚本未伪造数字）。根因同 ①：沙箱无游戏安装。**注意：该脚本失败时仍返回 EXIT=0 ⇒ 判成功必须看 `ok=True` 与指标，不能看退出码。**

## 3. 本夜最重要的三条工程结论

1. **预算门禁抓不到值级与模板级改动**：A9 语义改动、A10 模板回流的两次变异中，预算门禁**仍 EXIT 0**；真正咬住它们的是**宿主差分**与**文本缺席断言**。分层门禁不是冗余。
2. **同源码 ⇒ 同尺寸，但不 ⇒ 同 SHA**（四次实测）：差异来自 PE 头 TimeDateStamp，**且本树 `war3_perf_monitor.cpp:3839` 还有 `__DATE__/__TIME__` 构建戳** ⇒ 同源重链接本就不逐字节复现。**DLL 身份必须登记「最终树最后一次真实构建」**，一致性靠「源码逐字节相同 + ninja -n no work + 全门禁绿」证明。
3. **`-loadfile` 不足以进图**：这是本夜 T1 阻塞的唯一根因，且与启动器/绝对路径/相对路径无关，指向缺失的**地图部署环节**（短路径/复制）。

## 4. 诚实缺口（不得掩盖）

- **M1/M2 历史差分计数无法复验**：四条等价门禁（M1/M2/M2-3/M2-5）今晚全部 EXIT=0，但它们打印的是**断言数下限**（M1 `3000000`、M2 `2000000`）；M2-3/M2-5 打印的是**全文件累计实际值 40,215,908**（下限 12,000,000 / 21,000,000）。因此**不能声称**硬约束里的「M1 差分 3,098,194/0」「M2 链差分 10,357,597/0」在今晚树上被复验 —— 它们是更早记录的数字，今晚只验证了下限。**不得把「下限满足」当作「历史计数不变」。**

- **验证归属**：往返、记录器、成本、帧证据运行时均已由主线程亲跑；解析器 94/94 与根读方 55/55 起步为子代理证据，**次班已一手复跑确认可引用（见 §5.7.2 更正；本条目的夜间 ⚠️ 加注作废）**。
- 我本人一次调用错误：直接跑 5 个 runner 脚本未给位置参数 ⇒ argparse EXIT=2（**非树回归**）；已用正确参数重跑往返门禁通过。

- **F3 全部未执行**（无 pre-U5 基线、未启动游戏、未部署）；域维度计数**已接到既有出口**（perf monitor → `war3_perf_report.html` 内嵌 JSON；注：`runtime_status.json` 实测 0 命中本族，故未走该文件），但**出口代码未被可执行级测试覆盖** ⇒「实机 JSON 里确有这些键」仍未验证。
- Registry domain 隔离：D1/D3/D4/D5/D7 未动；三条 owner-check 变异未真跑（**出口块**的三条变异已于 06:48 补跑并关闭缺口）。
- **provenance 缺口**：`test_bridge_ramp_shadow_safety_static.py` 与 `test_shadow_lifecycle_tombstone_static.py` 的**锚点被更新**，护栏强度不变（取值由 domain 头 + 新门禁双重钉死），但**其 pre-change SHA 未捕获**（任务开始前已是 working-tree modified）。
- **流程副作用**：⑤ 第一块第一版 runner 占位符未替换，误以会话目录树为 CWD 跑了几条命令；该树误写日志已删除、其 `device.cpp` **mtime** 被 touch（**内容未变**），B 树那版结果已整体作废并重跑。
- M2-3 `motion-on` 与 M2-5 `diagnostics-on` 门禁的**打印数是全文件累计**（随迁移增长：37,920,642 → 39,268,450 → 40,215,908），下限仍满足、门禁未改，但相关记录的解读需更新。
- A9 迁出后在生产代码里**仍然 0 调用**（迁移 = 可审计的死代码，不等于被使用）。
- 全程**无 GPU/实机/像素/性能证据**；CPU 侧证据不得表述为 GPU 提交或画面正确性证明。

## 5. 下一步（按优先级）

1. ~~域维度计数对外出口~~（已完成）→ 用该出口做 **pre-U5 基线采集并登记**，之后才可能执行 F3。
2. **T1/T8 需要你二选一**：人工热键一次，或授权供给沙箱。
3. D4/D5 分账（先只做可读，cap/age 策略保持全局）；**D1 形态 (b)（key 级 domain）需裁定**；补跑三条 owner-check 变异。
4. M2-5 表 B 剩余项各有前置（B1/B2 需先外移 `War3SemanticSubmitScope`；B5 需先外移 `device.h` 内嵌类型；B11 等 M3）；A8 bounds 族调用点。
5. Stage13：等 Registry domain 隔离的 D 项闭合后再推。

## 5.1 流程事故（两次，均已如实记录）

1. ⑤ 第一块第一版 runner 占位符未替换 ⇒ 误以会话目录树为 CWD，该树误写日志已删、其 `device.cpp` **mtime** 被 touch（内容未变），B 树结果作废重跑。
2. 出口块 E1 第一次构建因后台作业默认 cwd = 会话工作区树，相对 `-C build32` 把编译落在**另一棵树的 build32**（本树未变）⇒ 该次作废，改用绝对路径 wrapper 重跑。**两棵树源码均未被触碰**。

**固化教训**：在后台作业里跑 ninja 必须用**绝对 build 目录**或显式 `workdir`。

## 5.2 时钟异常（影响时间戳解读）

本夜观测到**系统时钟向后跳约 15 分钟**（同一命令先后取时间为 `07:13` 与 `06:58`，疑似 NTP 校正，未做任何时钟/时区修改）⇒ 本文件与进度日志中的**时间戳不构成单调时间轴**；判断先后/耗时请用产物证据（SHA/字节/mtime/门禁输出）。另更正：A9 门禁的真实文件名是 `test_war3_palette_a9_migration_equivalence_static.py`（含 `migration`），七条等价门禁**全部**含在 `test_*_static.py` 的 256 个文件内。


## 5.3 停止点（07:30）之后的只读增量（重要，勿漏）

停止新工作后，主线程继续做了**纯只读**审计，产出了本轮最有价值的 T3 结论：

1. **`docs/plan/2026-09-18-t3-readonly-audit-record.md`（新增交付）**：合并 T3 只读审计全链 —— 缓存访问面 **9 处/单一 TU**、写面 **3 处确证**、读面 **6 处枚举对账**、发布协议（**全局序列号只写不读** + 逐条目 `writeSerial` 严格递增）、两端归属（写＝hook 抓取 / 读＝submit 选择链，`War3TryBuildLiveRuntimeGroupPalette` **5 个生产调用点全在 device.cpp**）；
2. **关键发现**：`war3_shadow_renderer_core.cpp:9568-9579` 显示 **2026-09-17「方案 B 第一部分」线程修复已在树中** —— 控制面 drain 在分离线程调用 `ensureLatestFrameBuilt()`，但在**推进边界**被所有者许可门 `consumePermissionGranted(false)` 拦住（所有者＝`hooks::GetMainLoopThreadId()`，三类拒绝计数）；
3. **执行证据（非仅读码）**：`war3_shadow_build_thread_gate_test.exe` **9/9 PASS**（含 **`C3 pipe-thread-drain -> NotOwner`**，即 T3 场景本身）、`war3_shadow_build_lifecycle_test.exe` **187/187 PASS**；
4. **因此 `t2-t3-review-verdict.md` 的 §3 判断前提已被更正（新增 §6）**：T3 性质由"待修复 UB"修订为"**已有结构性修复（2026-09-17）、缺运行时印证与范围枚举**"；剩余项＝(i) 间接路径、(ii) 实机印证（可观测量＝三类拒绝计数）、(iii) 其它共享非原子状态；
5. **(iii) 已在 T3 路径三模块内枚举**：生产者模块仅 **2 个**可变共享非原子静态量（其一即 T3 对象）；**读侧模块 0 个**；**drain 宿主模块的共享可变状态全为原子或 `thread_local`**；
6. **未改任何源码**；全部为静态只读 + 宿主测试执行证据；**不声称 T3 闭合、不声称运行时安全**。

> 本夜因此累计 **15 条** 2026-09-18 changelog 条目（含本次 T3 更正），自我纠正 **6 次**（最后一次更正了"data_collection 清单是运行期消费者"这一推断）。

## 5.4 更正：④ 的"taxonomy 计数对照门禁"**早已实现**（本夜第 7 次自我纠正）

此前我把「taxonomy 计数对照门禁设计」列为 ④ 待办 —— **错误**。`AutoTest/test_war3_palette_taxonomy_equivalence_static.py`（464 行，fan-closed 10 项检查）**已包含计数对照**：

- 第 4 项：34 字段**单一实现计数**（`d3d9_war3_scene.h` 1 处定义 / `device.cpp` 0 次 / 模块 `.cpp` 次数等于登记表，其中 4 个字段登记为 2）；
- 第 9 项（动态）：`--probe-taxonomy` **16 场景 × 2 env** 对照**独立期望值表**，diagnostics-off 必须全 0，module/legacy 逐字段相等。

实测（EXIT=0）：legacy 参考 **22,511 B / `A5333AC7E7F5A080…`**；模块正文与 legacy 正文**逐字节相同 = 18,937 B**；pre-M2-5 device.cpp **2,273,048 B / `D8211864549BBD83…`**；**diagnostics-on 总检查 40,215,908（下限 21,000,000）** ⇒ 动态段**确实执行**，且澄清了 `40,215,908` 的确切含义。

⇒ 该项应记为**已完成并已验收**，已从 ④ 待办中移除。

## 5.5 收尾补证（本轮自跑）：全量静态 **256/256** 于冻结树重跑通过

停止点后主线程**亲自重跑** `AutoTest/test_*_static.py` 全部 **256** 个：**PASS=256 / FAIL=0**，同刻 `ninja -C build32 -n` = **no work** ⇒ 冻结树未被触碰。该集合**包含七条等价门禁**（M1 / M2 / M2-3 / M2-5 / M2-4 / M2-5B / A9）。

## 5.6 收尾补证（本轮自跑）：meson **84/84**；至此头条门禁全部「同树、同刻、主线程自跑」

`meson test -C build32` ⇒ **EXIT=0 / Ok 84 / Fail 0**。结合 §5.5 的静态 **256/256**，最终冻结树上今晚引用的**每一个头条数字**均已由主线程亲自执行确认：静态 256/256 · meson 84/84 · 预算门禁（157/112/46/567）· 域 37 tests · 生命周期 187/187 · 线程闸门 9/9 · taxonomy 动态（16 场景×2 env，40,215,908）· 往返 CERTIFIED · 记录器 23/23 · 成本 PASS(38/38) · `ninja -n` no work。

**仍未亲跑的仅两项**：① M1/M2 历史差分计数（门禁只印下限，**不可再验证**，已如实登记）；② 解析器 94/94【⚠️ 全树无原始输出出处，见 §5.7，不得当已验数字】 与根读方 55/55【⚠️ 同上，未验证】（原为**子代理证据**，下一班应补跑）。

## 5.7 ⚠️ 完整性发现（**结论请连读 §5.7.1**）：这两个历史数字在全树都没有原始输出出处 —— 其中 **94/94 已确证**、**55/55 判定不可验证**

循「不传播未验证数字」的纪律，我对这两个被反复引用的数字做了溯源，结论如下：

| 检索 | 结果 |
| --- | --- |
| 全仓 `94/94` | 命中文件**全部是 `.md` 散文**（`DEVELOPMENT_CHANGELOG.md` 14 处、本夜 progress-log 18 处、本 summary 3 处、若干 record/handoff）；**`.log` 文件命中数 = 0** |
| `2026-09-18-m2-3-full-gate-rerun.log`（changelog 该条目**自己引用的原始输出**） | **0 命中** ⇒ 该条目写的「解析器静态 94/94、通用根读方静态 55/55」**并非来自它自己引用的日志** |
| 往返门禁输出 | 只打印 `ENCODER 73` / `ROUNDTRIP 116` / `CHECKS=1107`，**无 94/55** |
| `test_*_static.py` 256 集合 | 二者与之**并列**列出 ⇒ **不属于**该集合 |

⇒ 判定：这两个数字在本树是**沿用性散文断言（inherited prose claim）**，**没有可复现的原始输出**。

**据此采取的动作**：
1. 本 summary 中所有 `94/94`、`55/55` 出现处**已就地加注**「全树无原始输出出处，不得当已验数字」；
2. **不将二者列入"主线程已确认"**；交接文档已保留并强化该提示；
3. ⚠️ **包括我自己的 18 处台账引用在内**，本夜对它们的使用均属**转述**，不是验证 —— 本夜我**未能**把这一项变成已验证，选择**如实保留缺口**。


### 5.7.1 后续追查结果（同夜稍后，**部分确证**）

| 数字 | 状态 | 证据 |
| --- | --- | --- |
| **解析器静态 94/94** | **✅ 已确证** | `AutoTest/test_palette_object_evidence_analysis_static.py` ⇒ 主线程亲跑 **Ran 94 tests / OK（EXIT=0）** |
| 通用根读方静态 55/55 | **❌ 仍未定位** | 候选已排除：`test_analyze_frame_history.py` = **Ran 13 tests**（非 55）；12 个 `test_*static*.py` 中名称含 frame/history/reader/palette 者**输出均不含 55** |

**同时更正一条我此前的推断**：「94/94 不在 `test_*_static.py` 的 256 集合内」—— **错误**。`test_palette_object_evidence_analysis_static.py` 的文件名**正好匹配 `test_*_static.py`** ⇒ 它是 **256 集合的成员**；changelog 把它**单独列出**是为了强调，而非因为它不在集合内。

⇒ 因此「解析器静态 94/94」**可以放心引用**（我已亲跑该脚本 94/94 通过）；「通用根读方静态 55/55」**仍不得引用**（未定位、无原始输出）。

### 5.7.2 次班更正（2026-09-18 上午，审核班）：§5.7 与 §5.7.1 对 55/55 的判定**错误**，予以撤销

**更正方**：次班审核（用户指派），证据为本节两条一手实测。

1. **「全树无原始输出出处」是检索词假阴性**：unittest 输出格式为 `Ran N tests`，不含 `N/N` 字面串。
   `docs/plan/2026-09-18-m2-3-full-gate-rerun.log` 第 341 行 `Ran 94 tests in 0.134s`、第 348 行 `Ran 55 tests in 0.151s`
   —— 两个数字**昨夜就有原始输出出处**，且恰在该条目自己引用的日志里。
2. **55/55 本树可直接复现**（次班亲跑，2026-09-18 08:5x）：`py AutoTest/test_analyze_frame_evidence.py`
   ⇒ `Ran 55 tests in 0.085s / OK / EXIT=0`。该脚本 254 行、55 个测试方法
   （ReaderTests 37 + ExtensionVersionTests 8 + ScreenshotCanaryTests 10）、纯内存合成 fixture，不依赖任何实机产物。
3. 结论：**94/94 与 55/55 均为可引用的已验证数字**；§5.7 的判定与 §1 表、§5 中的 ⚠️ 加注作废（保留原文不删除，以本节为准）。
   附：M1 差分 exe 当前默认矩阵输出恰为 `3098194/3098194 checks passed`（该电池未增长，历史值=当前值，可复验）；
   不可单独复验的仅 M2-1 历史总值 10,357,597（同目标电池已加法式扩至 40,215,909）。

## 6. 未破坏的纪律

> **变更集基线（下一班必读）**：`git status --porcelain` 共 **583** 项 = tracked modified **102** + untracked **481**（`docs/plan/` 85、`AutoTest/` 185、`src/` 132，余为其它目录）；**绝大多数不是今晚产生的**（B 树原本就带大量未提交改动）。禁止 git 写 ⇒ 全部保持未提交。引用"本次改了什么"必须用文件级 SHA/字节/mtime 或文档列表，**不得**假设"git status 里都是本次改动"。

未 git 写 · 未部署 · **现场 `E:\Work\Warcraft III\d3d9.dll` 全程 `A0A51AF2…` 未替换** · 未触碰玩家目录内容（仅做被授权的启动游戏与只读检查）· 构建 Below Normal + `-j2` 且全机单构建 · 未称稳定版 · 未把 CPU 证据当 GPU 证据 · 未把隔离桌面数据当玩家前台性能。

## 7. 进度台账指针

- 逐轮台账：`docs/plan/2026-09-18-overnight-progress-log.md`（含每轮时间戳、根因判定、验收数字）
- 各任务记录：`2026-09-18-m2-3/-m2-4/-m2-5-taxonomy-extraction/-m2-5-remainder/-a9-a10-dead-code-ruling/-registry-domain-isolation` 等记录文档与对应 `*-full-gate*.log`、`*-mutation-raw-output.log`
- 勘察：`2026-09-18-t7-registry-stage13-survey.md`、`2026-09-18-m2-5-taxonomy-and-remaining-scope.md`
